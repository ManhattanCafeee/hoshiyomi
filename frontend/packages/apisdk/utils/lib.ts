// Re-exports from defaults.ts and to.ts were removed to prevent large auto-generated
// code (7000+ lines) from leaking into the bundle via auto-import scanning.
// These helpers ($default*, $full*, $init*, $to*) are unused in application code.
