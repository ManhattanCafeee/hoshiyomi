/* tslint:disable */
/* eslint-disable */
/**
 * hoshiyomi API - version 0.1.0
 *
 * hoshiyomi 后端 REST API
 *
 * OpenAPI version: 3.1.0
 *
 *
 * NOTE: This file is auto generated by the alova's vscode plugin.
 *
 * https://alova.js.org/devtools/vscode
 *
 * **Do not edit the file manually.**
 */
import type { Alova, AlovaMethodCreateConfig, AlovaGenerics, Method } from 'alova';
import type { $$userConfigMap, alovaInstance } from './index';
import type apiDefinitions from './apiDefinitions';

type CollapsedAlova = typeof alovaInstance;
type UserMethodConfigMap = typeof $$userConfigMap;

type Alova2MethodConfig<Responded> =
  CollapsedAlova extends Alova<
    AlovaGenerics<
      any,
      any,
      infer RequestConfig,
      infer Response,
      infer ResponseHeader,
      infer L1Cache,
      infer L2Cache,
      infer SE
    >
  >
    ? Omit<
        AlovaMethodCreateConfig<
          AlovaGenerics<Responded, any, RequestConfig, Response, ResponseHeader, L1Cache, L2Cache, SE>,
          any,
          Responded
        >,
        'params'
      >
    : never;

// Extract the return type of transform function that define in $$userConfigMap, if it not exists, use the default type.
type ExtractUserDefinedTransformed<
  DefinitionKey extends keyof typeof apiDefinitions,
  Default
> = DefinitionKey extends keyof UserMethodConfigMap
  ? UserMethodConfigMap[DefinitionKey]['transform'] extends (...args: any[]) => any
    ? Awaited<ReturnType<UserMethodConfigMap[DefinitionKey]['transform']>>
    : Default
  : Default;
type Alova2Method<
  Responded,
  DefinitionKey extends keyof typeof apiDefinitions,
  CurrentConfig extends Alova2MethodConfig<any>
> =
  CollapsedAlova extends Alova<
    AlovaGenerics<
      any,
      any,
      infer RequestConfig,
      infer Response,
      infer ResponseHeader,
      infer L1Cache,
      infer L2Cache,
      infer SE
    >
  >
    ? Method<
        AlovaGenerics<
          CurrentConfig extends undefined
            ? ExtractUserDefinedTransformed<DefinitionKey, Responded>
            : CurrentConfig['transform'] extends (...args: any[]) => any
              ? Awaited<ReturnType<CurrentConfig['transform']>>
              : ExtractUserDefinedTransformed<DefinitionKey, Responded>,
          any,
          RequestConfig,
          Response,
          ResponseHeader,
          L1Cache,
          L2Cache,
          SE
        >
      >
    : never;

export interface LoginReq {
  password: string;
  username: string;
}
export interface RefreshReq {
  refresh_token: string;
}
export interface RegisterReq {
  email: string;
  password: string;
  username: string;
}
export interface FieldViolation {
  /**
   * 失败的规则名(如 "length"、"email")
   */
  code: string;
  /**
   * DTO 为该规则声明的消息(声明了才有)
   */
  message?: string | null;
  /**
   * 失败规则的参数(如 {"min": 3, "max": 20})。提交的字段值刻意不回显:validator 会把它记为 params.value,原样回显会把原始输入(口令、令牌)返回给客户端。
   */
  params: Record<string, any>;
}
export type ValidationErrors = Record<string, FieldViolation[]>;
export interface ApiResponse_JwtEchoResp {
  /**
   * 0 表示成功,否则为失败的 HTTP 状态码
   */
  code: number;
  /**
   * JWT 认证检查的响应体
   */
  data?: {
    user_id: number;
    username: string;
  };
  errors?: null | ValidationErrors;
  /**
   * 简短的可读消息;成功时为 "ok"
   */
  message: string;
}
export interface ApiResponse_MessageResp {
  /**
   * 0 表示成功,否则为失败的 HTTP 状态码
   */
  code: number;
  /**
   * 通用消息响应体
   */
  data?: {
    message: string;
  };
  errors?: null | ValidationErrors;
  /**
   * 简短的可读消息;成功时为 "ok"
   */
  message: string;
}
export type Perm =
  '*' | 'user:read' | 'user:write' | 'user:delete' | 'user:*' | 'role:read' | 'role:write' | 'role:delete' | 'role:*';
export interface UserResp {
  created_at: string;
  email: string;
  id: number;
  updated_at: string;
  username: string;
}
export interface ApiResponse_AuthStateResp {
  /**
   * 0 表示成功,否则为失败的 HTTP 状态码
   */
  code: number;
  /**
   * 当前认证状态:用户信息与权限码
   */
  data?: {
    permissions: Perm[];
    /**
     * 用户响应体(不含口令散列)
     */
    user: UserResp;
  };
  errors?: null | ValidationErrors;
  /**
   * 简短的可读消息;成功时为 "ok"
   */
  message: string;
}
export interface ApiResponse_String {
  /**
   * 0 表示成功,否则为失败的 HTTP 状态码
   */
  code: number;
  data?: string;
  errors?: null | ValidationErrors;
  /**
   * 简短的可读消息;成功时为 "ok"
   */
  message: string;
}
export interface ApiResponse_UserResp {
  /**
   * 0 表示成功,否则为失败的 HTTP 状态码
   */
  code: number;
  /**
   * 用户响应体(不含口令散列)
   */
  data?: {
    created_at: string;
    email: string;
    id: number;
    updated_at: string;
    username: string;
  };
  errors?: null | ValidationErrors;
  /**
   * 简短的可读消息;成功时为 "ok"
   */
  message: string;
}
export interface ChangePasswordReq {
  new_password: string;
  old_password: string;
}
export interface UpdateUsernameReq {
  username: string;
}
export interface AuthStateResp {
  permissions: Perm[];
  /**
   * 用户响应体(不含口令散列)
   */
  user: UserResp;
}
export interface ApiResponse_LoginResp {
  /**
   * 0 表示成功,否则为失败的 HTTP 状态码
   */
  code: number;
  /**
   * JWT 登录响应:令牌对与当前认证状态
   */
  data?: {
    access_token: string;
    refresh_token: string;
    /**
     * 当前认证状态:用户信息与权限码
     */
    state: AuthStateResp;
  };
  errors?: null | ValidationErrors;
  /**
   * 简短的可读消息;成功时为 "ok"
   */
  message: string;
}
export interface ApiResponse_RefreshResp {
  /**
   * 0 表示成功,否则为失败的 HTTP 状态码
   */
  code: number;
  /**
   * 刷新响应:轮换后的新令牌对
   */
  data?: {
    access_token: string;
    refresh_token: string;
  };
  errors?: null | ValidationErrors;
  /**
   * 简短的可读消息;成功时为 "ok"
   */
  message: string;
}
export interface ApiResponse_PageData_UserResp {
  /**
   * 0 表示成功,否则为失败的 HTTP 状态码
   */
  code: number;
  /**
   * 分页数据载荷(库的 `Page<T>` 映射而来)
   */
  data?: {
    /**
     * 本页数据
     */
    items: Array<{
      created_at: string;
      email: string;
      id: number;
      updated_at: string;
      username: string;
    }>;
    /**
     * 当前页码(回显请求值)
     */
    page: number;
    /**
     * 每页条数(回显请求值)
     */
    per_page: number;
    /**
     * 记录总数
     */
    total: number;
  };
  errors?: null | ValidationErrors;
  /**
   * 简短的可读消息;成功时为 "ok"
   */
  message: string;
}
declare global {
  interface Apis {
    auth: {
      /**
       * ---
       *
       * [GET]
       *
       * **path:** /api/v1/auth/jwt/echo
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 0 表示成功,否则为失败的 HTTP 状态码
       *   code: number
       *   // JWT 认证检查的响应体
       *   data?: {
       *     user_id: number
       *     username: string
       *   }
       *   // [params2] start
       *   // 校验失败明细,仅在校验失败时出现
       *   // [params2] end
       *   errors?: null | Record<
       *     string,
       *     Array<{
       *       // 失败的规则名(如 "length"、"email")
       *       code: string
       *       // DTO 为该规则声明的消息(声明了才有)
       *       message?: string | null
       *       // 失败规则的参数(如 {"min": 3, "max": 20})。提交的字段值刻意不回显:validator 会把它记为 params.value,原样回显会把原始输入(口令、令牌)返回给客户端。
       *       params: Record<string, any>
       *     }>
       *   >
       *   // 简短的可读消息;成功时为 "ok"
       *   message: string
       * }
       * ```
       */
      Auth__jwtEcho<Config extends Alova2MethodConfig<ApiResponse_JwtEchoResp>>(
        config?: Config
      ): Alova2Method<ApiResponse_JwtEchoResp, 'auth.Auth__jwtEcho', Config>;
      /**
       * ---
       *
       * [POST]
       *
       * **path:** /api/v1/auth/jwt/login
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   password: string
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 0 表示成功,否则为失败的 HTTP 状态码
       *   code: number
       *   // JWT 登录响应:令牌对与当前认证状态
       *   data?: {
       *     access_token: string
       *     refresh_token: string
       *     // 当前认证状态:用户信息与权限码
       *     state: {
       *       // [items] start
       *       // 权限点:serde 与 strum 序列化码必须一致(见 tests::test_permissions_consistency)
       *       // [items] end
       *       permissions: (
       *         | '*'
       *         | 'user:read'
       *         | 'user:write'
       *         | 'user:delete'
       *         | 'user:*'
       *         | 'role:read'
       *         | 'role:write'
       *         | 'role:delete'
       *         | 'role:*'
       *       )[]
       *       // 用户响应体(不含口令散列)
       *       user: {
       *         created_at: string
       *         email: string
       *         id: number
       *         updated_at: string
       *         username: string
       *       }
       *     }
       *   }
       *   // [params2] start
       *   // 校验失败明细,仅在校验失败时出现
       *   // [params2] end
       *   errors?: null | Record<
       *     string,
       *     Array<{
       *       // 失败的规则名(如 "length"、"email")
       *       code: string
       *       // DTO 为该规则声明的消息(声明了才有)
       *       message?: string | null
       *       // 失败规则的参数(如 {"min": 3, "max": 20})。提交的字段值刻意不回显:validator 会把它记为 params.value,原样回显会把原始输入(口令、令牌)返回给客户端。
       *       params: Record<string, any>
       *     }>
       *   >
       *   // 简短的可读消息;成功时为 "ok"
       *   message: string
       * }
       * ```
       */
      Auth__jwtLogin<
        Config extends Alova2MethodConfig<ApiResponse_LoginResp> & {
          data: LoginReq;
        }
      >(
        config: Config
      ): Alova2Method<ApiResponse_LoginResp, 'auth.Auth__jwtLogin', Config>;
      /**
       * ---
       *
       * [POST]
       *
       * **path:** /api/v1/auth/jwt/logout
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 0 表示成功,否则为失败的 HTTP 状态码
       *   code: number
       *   // 通用消息响应体
       *   data?: {
       *     message: string
       *   }
       *   // [params2] start
       *   // 校验失败明细,仅在校验失败时出现
       *   // [params2] end
       *   errors?: null | Record<
       *     string,
       *     Array<{
       *       // 失败的规则名(如 "length"、"email")
       *       code: string
       *       // DTO 为该规则声明的消息(声明了才有)
       *       message?: string | null
       *       // 失败规则的参数(如 {"min": 3, "max": 20})。提交的字段值刻意不回显:validator 会把它记为 params.value,原样回显会把原始输入(口令、令牌)返回给客户端。
       *       params: Record<string, any>
       *     }>
       *   >
       *   // 简短的可读消息;成功时为 "ok"
       *   message: string
       * }
       * ```
       */
      Auth__jwtLogout<Config extends Alova2MethodConfig<ApiResponse_MessageResp>>(
        config?: Config
      ): Alova2Method<ApiResponse_MessageResp, 'auth.Auth__jwtLogout', Config>;
      /**
       * ---
       *
       * [GET]
       *
       * **path:** /api/v1/auth/jwt/me
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 0 表示成功,否则为失败的 HTTP 状态码
       *   code: number
       *   // 当前认证状态:用户信息与权限码
       *   data?: {
       *     // [items] start
       *     // 权限点:serde 与 strum 序列化码必须一致(见 tests::test_permissions_consistency)
       *     // [items] end
       *     permissions: (
       *       | '*'
       *       | 'user:read'
       *       | 'user:write'
       *       | 'user:delete'
       *       | 'user:*'
       *       | 'role:read'
       *       | 'role:write'
       *       | 'role:delete'
       *       | 'role:*'
       *     )[]
       *     // 用户响应体(不含口令散列)
       *     user: {
       *       created_at: string
       *       email: string
       *       id: number
       *       updated_at: string
       *       username: string
       *     }
       *   }
       *   // [params2] start
       *   // 校验失败明细,仅在校验失败时出现
       *   // [params2] end
       *   errors?: null | Record<
       *     string,
       *     Array<{
       *       // 失败的规则名(如 "length"、"email")
       *       code: string
       *       // DTO 为该规则声明的消息(声明了才有)
       *       message?: string | null
       *       // 失败规则的参数(如 {"min": 3, "max": 20})。提交的字段值刻意不回显:validator 会把它记为 params.value,原样回显会把原始输入(口令、令牌)返回给客户端。
       *       params: Record<string, any>
       *     }>
       *   >
       *   // 简短的可读消息;成功时为 "ok"
       *   message: string
       * }
       * ```
       */
      Auth__jwtMe<Config extends Alova2MethodConfig<ApiResponse_AuthStateResp>>(
        config?: Config
      ): Alova2Method<ApiResponse_AuthStateResp, 'auth.Auth__jwtMe', Config>;
      /**
       * ---
       *
       * [POST]
       *
       * **path:** /api/v1/auth/jwt/refresh
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   refresh_token: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 0 表示成功,否则为失败的 HTTP 状态码
       *   code: number
       *   // 刷新响应:轮换后的新令牌对
       *   data?: {
       *     access_token: string
       *     refresh_token: string
       *   }
       *   // [params2] start
       *   // 校验失败明细,仅在校验失败时出现
       *   // [params2] end
       *   errors?: null | Record<
       *     string,
       *     Array<{
       *       // 失败的规则名(如 "length"、"email")
       *       code: string
       *       // DTO 为该规则声明的消息(声明了才有)
       *       message?: string | null
       *       // 失败规则的参数(如 {"min": 3, "max": 20})。提交的字段值刻意不回显:validator 会把它记为 params.value,原样回显会把原始输入(口令、令牌)返回给客户端。
       *       params: Record<string, any>
       *     }>
       *   >
       *   // 简短的可读消息;成功时为 "ok"
       *   message: string
       * }
       * ```
       */
      Auth__jwtRefresh<
        Config extends Alova2MethodConfig<ApiResponse_RefreshResp> & {
          data: RefreshReq;
        }
      >(
        config: Config
      ): Alova2Method<ApiResponse_RefreshResp, 'auth.Auth__jwtRefresh', Config>;
      /**
       * ---
       *
       * [POST]
       *
       * **path:** /api/v1/auth/login
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   password: string
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 0 表示成功,否则为失败的 HTTP 状态码
       *   code: number
       *   // 当前认证状态:用户信息与权限码
       *   data?: {
       *     // [items] start
       *     // 权限点:serde 与 strum 序列化码必须一致(见 tests::test_permissions_consistency)
       *     // [items] end
       *     permissions: (
       *       | '*'
       *       | 'user:read'
       *       | 'user:write'
       *       | 'user:delete'
       *       | 'user:*'
       *       | 'role:read'
       *       | 'role:write'
       *       | 'role:delete'
       *       | 'role:*'
       *     )[]
       *     // 用户响应体(不含口令散列)
       *     user: {
       *       created_at: string
       *       email: string
       *       id: number
       *       updated_at: string
       *       username: string
       *     }
       *   }
       *   // [params2] start
       *   // 校验失败明细,仅在校验失败时出现
       *   // [params2] end
       *   errors?: null | Record<
       *     string,
       *     Array<{
       *       // 失败的规则名(如 "length"、"email")
       *       code: string
       *       // DTO 为该规则声明的消息(声明了才有)
       *       message?: string | null
       *       // 失败规则的参数(如 {"min": 3, "max": 20})。提交的字段值刻意不回显:validator 会把它记为 params.value,原样回显会把原始输入(口令、令牌)返回给客户端。
       *       params: Record<string, any>
       *     }>
       *   >
       *   // 简短的可读消息;成功时为 "ok"
       *   message: string
       * }
       * ```
       */
      Auth__login<
        Config extends Alova2MethodConfig<ApiResponse_AuthStateResp> & {
          data: LoginReq;
        }
      >(
        config: Config
      ): Alova2Method<ApiResponse_AuthStateResp, 'auth.Auth__login', Config>;
      /**
       * ---
       *
       * [POST]
       *
       * **path:** /api/v1/auth/logout
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 0 表示成功,否则为失败的 HTTP 状态码
       *   code: number
       *   // 通用消息响应体
       *   data?: {
       *     message: string
       *   }
       *   // [params2] start
       *   // 校验失败明细,仅在校验失败时出现
       *   // [params2] end
       *   errors?: null | Record<
       *     string,
       *     Array<{
       *       // 失败的规则名(如 "length"、"email")
       *       code: string
       *       // DTO 为该规则声明的消息(声明了才有)
       *       message?: string | null
       *       // 失败规则的参数(如 {"min": 3, "max": 20})。提交的字段值刻意不回显:validator 会把它记为 params.value,原样回显会把原始输入(口令、令牌)返回给客户端。
       *       params: Record<string, any>
       *     }>
       *   >
       *   // 简短的可读消息;成功时为 "ok"
       *   message: string
       * }
       * ```
       */
      Auth__logout<Config extends Alova2MethodConfig<ApiResponse_MessageResp>>(
        config?: Config
      ): Alova2Method<ApiResponse_MessageResp, 'auth.Auth__logout', Config>;
      /**
       * ---
       *
       * [GET]
       *
       * **path:** /api/v1/auth/me
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 0 表示成功,否则为失败的 HTTP 状态码
       *   code: number
       *   // 当前认证状态:用户信息与权限码
       *   data?: {
       *     // [items] start
       *     // 权限点:serde 与 strum 序列化码必须一致(见 tests::test_permissions_consistency)
       *     // [items] end
       *     permissions: (
       *       | '*'
       *       | 'user:read'
       *       | 'user:write'
       *       | 'user:delete'
       *       | 'user:*'
       *       | 'role:read'
       *       | 'role:write'
       *       | 'role:delete'
       *       | 'role:*'
       *     )[]
       *     // 用户响应体(不含口令散列)
       *     user: {
       *       created_at: string
       *       email: string
       *       id: number
       *       updated_at: string
       *       username: string
       *     }
       *   }
       *   // [params2] start
       *   // 校验失败明细,仅在校验失败时出现
       *   // [params2] end
       *   errors?: null | Record<
       *     string,
       *     Array<{
       *       // 失败的规则名(如 "length"、"email")
       *       code: string
       *       // DTO 为该规则声明的消息(声明了才有)
       *       message?: string | null
       *       // 失败规则的参数(如 {"min": 3, "max": 20})。提交的字段值刻意不回显:validator 会把它记为 params.value,原样回显会把原始输入(口令、令牌)返回给客户端。
       *       params: Record<string, any>
       *     }>
       *   >
       *   // 简短的可读消息;成功时为 "ok"
       *   message: string
       * }
       * ```
       */
      Auth__me<Config extends Alova2MethodConfig<ApiResponse_AuthStateResp>>(
        config?: Config
      ): Alova2Method<ApiResponse_AuthStateResp, 'auth.Auth__me', Config>;
      /**
       * ---
       *
       * [POST]
       *
       * **path:** /api/v1/auth/register
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   email: string
       *   password: string
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 0 表示成功,否则为失败的 HTTP 状态码
       *   code: number
       *   // 用户响应体(不含口令散列)
       *   data?: {
       *     created_at: string
       *     email: string
       *     id: number
       *     updated_at: string
       *     username: string
       *   }
       *   // [params2] start
       *   // 校验失败明细,仅在校验失败时出现
       *   // [params2] end
       *   errors?: null | Record<
       *     string,
       *     Array<{
       *       // 失败的规则名(如 "length"、"email")
       *       code: string
       *       // DTO 为该规则声明的消息(声明了才有)
       *       message?: string | null
       *       // 失败规则的参数(如 {"min": 3, "max": 20})。提交的字段值刻意不回显:validator 会把它记为 params.value,原样回显会把原始输入(口令、令牌)返回给客户端。
       *       params: Record<string, any>
       *     }>
       *   >
       *   // 简短的可读消息;成功时为 "ok"
       *   message: string
       * }
       * ```
       */
      Auth__register<
        Config extends Alova2MethodConfig<ApiResponse_UserResp> & {
          data: RegisterReq;
        }
      >(
        config: Config
      ): Alova2Method<ApiResponse_UserResp, 'auth.Auth__register', Config>;
    };
    health: {
      /**
       * ---
       *
       * [GET]
       *
       * **path:** /api/v1/health
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 0 表示成功,否则为失败的 HTTP 状态码
       *   code: number
       *   data?: string
       *   // [params2] start
       *   // 校验失败明细,仅在校验失败时出现
       *   // [params2] end
       *   errors?: null | Record<
       *     string,
       *     Array<{
       *       // 失败的规则名(如 "length"、"email")
       *       code: string
       *       // DTO 为该规则声明的消息(声明了才有)
       *       message?: string | null
       *       // 失败规则的参数(如 {"min": 3, "max": 20})。提交的字段值刻意不回显:validator 会把它记为 params.value,原样回显会把原始输入(口令、令牌)返回给客户端。
       *       params: Record<string, any>
       *     }>
       *   >
       *   // 简短的可读消息;成功时为 "ok"
       *   message: string
       * }
       * ```
       */
      Health__check<Config extends Alova2MethodConfig<ApiResponse_String>>(
        config?: Config
      ): Alova2Method<ApiResponse_String, 'health.Health__check', Config>;
    };
    user: {
      /**
       * ---
       *
       * [GET]
       *
       * **path:** /api/v1/users
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // 页码,从 1 开始
       *   page?: number
       *   // 每页条数,1-100
       *   per_page?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 0 表示成功,否则为失败的 HTTP 状态码
       *   code: number
       *   // 分页数据载荷(库的 `Page<T>` 映射而来)
       *   data?: {
       *     // 本页数据
       *     // [items] start
       *     // 用户响应体(不含口令散列)
       *     // [items] end
       *     items: Array<{
       *       created_at: string
       *       email: string
       *       id: number
       *       updated_at: string
       *       username: string
       *     }>
       *     // 当前页码(回显请求值)
       *     page: number
       *     // 每页条数(回显请求值)
       *     per_page: number
       *     // 记录总数
       *     total: number
       *   }
       *   // [params2] start
       *   // 校验失败明细,仅在校验失败时出现
       *   // [params2] end
       *   errors?: null | Record<
       *     string,
       *     Array<{
       *       // 失败的规则名(如 "length"、"email")
       *       code: string
       *       // DTO 为该规则声明的消息(声明了才有)
       *       message?: string | null
       *       // 失败规则的参数(如 {"min": 3, "max": 20})。提交的字段值刻意不回显:validator 会把它记为 params.value,原样回显会把原始输入(口令、令牌)返回给客户端。
       *       params: Record<string, any>
       *     }>
       *   >
       *   // 简短的可读消息;成功时为 "ok"
       *   message: string
       * }
       * ```
       */
      User__list<
        Config extends Alova2MethodConfig<ApiResponse_PageData_UserResp> & {
          params: {
            /**
             * 页码,从 1 开始
             */
            page?: number;
            /**
             * 每页条数,1-100
             */
            per_page?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<ApiResponse_PageData_UserResp, 'user.User__list', Config>;
      /**
       * ---
       *
       * [POST]
       *
       * **path:** /api/v1/users
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 0 表示成功,否则为失败的 HTTP 状态码
       *   code: number
       *   // 用户响应体(不含口令散列)
       *   data?: {
       *     created_at: string
       *     email: string
       *     id: number
       *     updated_at: string
       *     username: string
       *   }
       *   // [params2] start
       *   // 校验失败明细,仅在校验失败时出现
       *   // [params2] end
       *   errors?: null | Record<
       *     string,
       *     Array<{
       *       // 失败的规则名(如 "length"、"email")
       *       code: string
       *       // DTO 为该规则声明的消息(声明了才有)
       *       message?: string | null
       *       // 失败规则的参数(如 {"min": 3, "max": 20})。提交的字段值刻意不回显:validator 会把它记为 params.value,原样回显会把原始输入(口令、令牌)返回给客户端。
       *       params: Record<string, any>
       *     }>
       *   >
       *   // 简短的可读消息;成功时为 "ok"
       *   message: string
       * }
       * ```
       */
      User__create<Config extends Alova2MethodConfig<ApiResponse_UserResp>>(
        config?: Config
      ): Alova2Method<ApiResponse_UserResp, 'user.User__create', Config>;
      /**
       * ---
       *
       * [GET]
       *
       * **path:** /api/v1/users/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 用户 ID
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 0 表示成功,否则为失败的 HTTP 状态码
       *   code: number
       *   // 用户响应体(不含口令散列)
       *   data?: {
       *     created_at: string
       *     email: string
       *     id: number
       *     updated_at: string
       *     username: string
       *   }
       *   // [params2] start
       *   // 校验失败明细,仅在校验失败时出现
       *   // [params2] end
       *   errors?: null | Record<
       *     string,
       *     Array<{
       *       // 失败的规则名(如 "length"、"email")
       *       code: string
       *       // DTO 为该规则声明的消息(声明了才有)
       *       message?: string | null
       *       // 失败规则的参数(如 {"min": 3, "max": 20})。提交的字段值刻意不回显:validator 会把它记为 params.value,原样回显会把原始输入(口令、令牌)返回给客户端。
       *       params: Record<string, any>
       *     }>
       *   >
       *   // 简短的可读消息;成功时为 "ok"
       *   message: string
       * }
       * ```
       */
      User__get<
        Config extends Alova2MethodConfig<ApiResponse_UserResp> & {
          pathParams: {
            /**
             * 用户 ID
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<ApiResponse_UserResp, 'user.User__get', Config>;
      /**
       * ---
       *
       * [DELETE]
       *
       * **path:** /api/v1/users/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 用户 ID
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 0 表示成功,否则为失败的 HTTP 状态码
       *   code: number
       *   // 通用消息响应体
       *   data?: {
       *     message: string
       *   }
       *   // [params2] start
       *   // 校验失败明细,仅在校验失败时出现
       *   // [params2] end
       *   errors?: null | Record<
       *     string,
       *     Array<{
       *       // 失败的规则名(如 "length"、"email")
       *       code: string
       *       // DTO 为该规则声明的消息(声明了才有)
       *       message?: string | null
       *       // 失败规则的参数(如 {"min": 3, "max": 20})。提交的字段值刻意不回显:validator 会把它记为 params.value,原样回显会把原始输入(口令、令牌)返回给客户端。
       *       params: Record<string, any>
       *     }>
       *   >
       *   // 简短的可读消息;成功时为 "ok"
       *   message: string
       * }
       * ```
       */
      User__delete<
        Config extends Alova2MethodConfig<ApiResponse_MessageResp> & {
          pathParams: {
            /**
             * 用户 ID
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<ApiResponse_MessageResp, 'user.User__delete', Config>;
      /**
       * ---
       *
       * [PUT]
       *
       * **path:** /api/v1/users/{id}/password
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 用户 ID
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   new_password: string
       *   old_password: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 0 表示成功,否则为失败的 HTTP 状态码
       *   code: number
       *   // 通用消息响应体
       *   data?: {
       *     message: string
       *   }
       *   // [params2] start
       *   // 校验失败明细,仅在校验失败时出现
       *   // [params2] end
       *   errors?: null | Record<
       *     string,
       *     Array<{
       *       // 失败的规则名(如 "length"、"email")
       *       code: string
       *       // DTO 为该规则声明的消息(声明了才有)
       *       message?: string | null
       *       // 失败规则的参数(如 {"min": 3, "max": 20})。提交的字段值刻意不回显:validator 会把它记为 params.value,原样回显会把原始输入(口令、令牌)返回给客户端。
       *       params: Record<string, any>
       *     }>
       *   >
       *   // 简短的可读消息;成功时为 "ok"
       *   message: string
       * }
       * ```
       */
      User__changePassword<
        Config extends Alova2MethodConfig<ApiResponse_MessageResp> & {
          pathParams: {
            /**
             * 用户 ID
             */
            id: number;
          };
          data: ChangePasswordReq;
        }
      >(
        config: Config
      ): Alova2Method<ApiResponse_MessageResp, 'user.User__changePassword', Config>;
      /**
       * ---
       *
       * [PUT]
       *
       * **path:** /api/v1/users/{id}/username
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // 用户 ID
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // 0 表示成功,否则为失败的 HTTP 状态码
       *   code: number
       *   // 用户响应体(不含口令散列)
       *   data?: {
       *     created_at: string
       *     email: string
       *     id: number
       *     updated_at: string
       *     username: string
       *   }
       *   // [params2] start
       *   // 校验失败明细,仅在校验失败时出现
       *   // [params2] end
       *   errors?: null | Record<
       *     string,
       *     Array<{
       *       // 失败的规则名(如 "length"、"email")
       *       code: string
       *       // DTO 为该规则声明的消息(声明了才有)
       *       message?: string | null
       *       // 失败规则的参数(如 {"min": 3, "max": 20})。提交的字段值刻意不回显:validator 会把它记为 params.value,原样回显会把原始输入(口令、令牌)返回给客户端。
       *       params: Record<string, any>
       *     }>
       *   >
       *   // 简短的可读消息;成功时为 "ok"
       *   message: string
       * }
       * ```
       */
      User__updateUsername<
        Config extends Alova2MethodConfig<ApiResponse_UserResp> & {
          pathParams: {
            /**
             * 用户 ID
             */
            id: number;
          };
          data: UpdateUsernameReq;
        }
      >(
        config: Config
      ): Alova2Method<ApiResponse_UserResp, 'user.User__updateUsername', Config>;
    };
  }

  var Apis: Apis;
}
